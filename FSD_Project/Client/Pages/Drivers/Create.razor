@page "/drivers/create"
@inject HttpClient _client
@inject NavigationManager _navManager
@attribute [Authorize]


<h3 class="driverd-title">Create New Drivers</h3>
<hr />
<br />

<FormComponent ButtonText="Create Driver" driver="driver" OnValidSubmit="@CreateDriver" />


@code {
    Driver driver = new Driver();

    private async Task CreateDriver()
    {
        await _client.PostAsJsonAsync(Endpoints.DriversEndpoint, driver);
        _navManager.NavigateTo("/drivers/");
    }
}
